{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:circuit:compile:register": "mkdir -p build/circuits && npx circom2 circuits/register.circom --r1cs --wasm --sym -o build/circuits",
    "build:circuit:compile:commitment": "mkdir -p build/circuits && npx circom2 circuits/commitment.circom --r1cs --wasm --sym -o build/circuits",
    "build:circuit:compile": "npm run build:circuit:compile:register && npm run build:circuit:compile:commitment",
    "build:circuit:info:register": "npx snarkjs r1cs info build/circuits/register.r1cs",
    "build:circuit:info:commitment": "npx snarkjs r1cs info build/circuits/commitment.r1cs",
    "build:circuit:info": "npm run build:circuit:info:register && npm run build:circuit:info:commitment",
    "build:circuit:setup:plonk": "node scripts/trust-setup-plonk.js",
    "build:circuit:setup:groth16": "node scripts/trust-setup-groth16.js",
    "build:circuit:setup": "npm run build:circuit:setup:groth16",
    "build:circuit:export:verificationkey:register": "npx snarkjs zkey export verificationkey build/circuits/register_final.zkey build/circuits/register_vk.json ",
    "build:circuit:export:verificationkey:commitment": "npx snarkjs zkey export verificationkey build/circuits/commitment_final.zkey build/circuits/commitment_vk.json ",
    "build:circuit:export:verificationkey": "npm run build:circuit:export:verificationkey:register && npm run build:circuit:export:verificationkey:commitment",
    "build:circuit:export:solidityverifier:register:plonk": "npx snarkjs zkey export solidityverifier build/circuits/register_final.zkey contracts/RegisterPlonkVerifier.sol",
    "build:circuit:export:solidityverifier:commitment:plonk": "npx snarkjs zkey export solidityverifier build/circuits/commitment_final.zkey contracts/CommitmentPlonkVerifier.sol",
    "build:circuit:export:solidityverifier:register:groth16": "npx snarkjs zkey export solidityverifier build/circuits/register_final.zkey contracts/RegisterGroth16Verifier.sol ",
    "build:circuit:export:solidityverifier:commitment:groth16": "npx snarkjs zkey export solidityverifier build/circuits/commitment_final.zkey contracts/CommitmentGroth16Verifier.sol",
    "build:circuit:export:solidityverifier": "mkdir -p build/contracts && npm run build:circuit:export:solidityverifier:register:groth16 && npm run build:circuit:export:solidityverifier:commitment:groth16 && node scripts/customizeVerifierName.js",
    "build:circuit:register": "npm run build:circuit:compile:register && npm run build:circuit:info:register && npm run build:circuit:setup && npm run build:circuit:export:verificationkey:register && npm run build:circuit:export:solidityverifier:register:groth16",
    "build:circuit:commitment": "npm run build:circuit:compile:commitment && npm run build:circuit:info:commitment && npm run build:circuit:setup && npm run build:circuit:export:verificationkey:commitment && npm run build:circuit:export:solidityverifier:commitment:groth16",
    "build:circuit": "npm run build:circuit:compile && npm run build:circuit:info && npm run build:circuit:setup && npm run build:circuit:export:verificationkey && npm run build:circuit:export:solidityverifier",
    "build": "npm run build:circuit",
    "deploy": "npx hardhat run scripts/deploy.js --network polygonAmoy",
    "runner:create": "npx hardhat run scripts/userManagement/createRunner.js --network polygonAmoy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.9",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv-cli": "^8.0.0",
    "ethers": "^6.13.4",
    "ffjavascript": "^0.3.1",
    "hardhat": "^2.22.13",
    "mocha": "^10.7.3"
  },
  "dependencies": {
    "@account-abstraction/contracts": "^0.7.0",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.8",
    "circom_tester": "^0.0.20",
    "circom2": "^0.2.19",
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "crypto": "^1.0.1",
    "dotenv": "^16.4.5",
    "fixed-merkle-tree": "^0.7.3",
    "snarkjs": "^0.7.5"
  }
}